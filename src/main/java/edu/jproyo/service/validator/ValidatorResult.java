package edu.jproyo.service.validator;

/**
 * Validation possible results
 */
public enum ValidatorResult {
    /**
     * Valid validator result.
     */
    VALID,
    /**
     * Old validator result.
     */
    OLD,
    /**
     * Future validator result.
     */
    FUTURE,
    /**
     * Invalid validator result.
     */
    INVALID
}
